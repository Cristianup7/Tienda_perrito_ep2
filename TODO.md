## EP2 - Contenedorización + CI/CD (ToDo)

- [x] Crear `docker-compose.yml` estándar en la raíz (para CI/CD) a partir de `dockers-compose.yml/YAML`.
- [x] Robustecer `backend/server.js` (manejo si el pool MySQL no inicializa).



- [ ] Agregar workflows GitHub Actions en `backend/` y `frontend/` con trigger `deploy` (build → push → deploy EC2).
- [ ] Documentar `README.md` y variables/secrets requeridos.


